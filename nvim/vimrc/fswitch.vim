" Switch between header and source

" Switch the current file and load it into the buffer
nmap <silent> <Leader>of :FSHere<cr>
" Split the current buffer and load it on right
nmap <silent> <Leader>oR :FSSplitRight<cr>
" Split the current buffer and load it on left
nmap <silent> <Leader>oL :FSSplitLeft<cr>
