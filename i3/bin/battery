#!/bin/bash

online=$( cat /sys/class/power_supply/AC/online )
level=$( cat /sys/class/power_supply/BAT0/capacity )

if [ -z $level ]; then exit 0; fi
if [ $online == 1 ]; then echo -n "AC "; fi


echo "${level}%"
